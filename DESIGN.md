# 分层沉淀体系 · 设计

> 一套让 Claude Code 插件/能力「分层沉淀 + 自动防腐烂」的引擎设计与实测结论。
> 本文是**当前态设计**（原理、不变量、失败模式），不含任何组织/项目语境——目标是任何人可照此搭一套。
> 演进过程与逐条调查见 git 历史；未接入项见 `health/BACKLOG.md`。

## 一、要解决的问题

能力会腐烂：**没人用的还留着**、**被多个项目重复实现的没上移**、**做得好不好没人知道**。
而「靠人定期检查」必然失败——人会忘、也不会每次认真看（pull 模式的固有缺陷）。

治本方向：**push 模式**——机器不停歇地检测并产出「例外队列」，人只做低频、高价值的**裁决**。

## 二、三层划分

| 层 | 内容特征 | 载体 | 作用域 |
|---|---|---|---|
| 公共通用 | 不含业务、不含团队语境 | 公共 marketplace 仓库 | user 级 |
| 团队共用 | 有团队语境（团队约定、内部工具） | 团队 marketplace 仓库 | user 级 |
| 项目业务 | 绑死本项目 | 项目 `.claude/` | project 级 |

**不变式**：依赖单向 `项目业务 → 团队共用 → 公共通用`，禁止反向引用。

**唯一准入判据**：*残留任何团队/项目影子*（专名、机型、写死路径、内部服务名），就没资格往上走。

**适用范围**：该判据适用于某层的**任何文件**——代码、文档、数据一视同仁，不只是插件内容。

## 三、多平台可移植性

| 层 | 可移植性 |
|---|---|
| 自动化引擎（脚本 + 直接调 LLM） | ✅ 与编辑器无关 |
| **Skill 内容（`SKILL.md`）** | ✅ **开放标准**（agentskills.io），多编辑器原生兼容 |
| 运行时（MCP） | ✅ 跨工具标准 |
| 项目说明（`CLAUDE.md` / `AGENTS.md`） | ⚠️ 两个并存标准，内容可转 |
| 打包层（marketplace / plugin.json）、子代理 frontmatter、hooks | ❌ 绑死 Claude Code |

**三原则**：① **Skill 是主载体**（唯一「写一次、多平台原生跑」的形态）；② 内容层去工具化（纯 markdown 单一事实源）；③ 绑死平台的能力不预建适配器（YAGNI），需要时从内容层重建。

## 四、三条流水线

### A. 转录解析（用量侧，已实施）

**数据源**：Claude Code 的会话转录（`<config>/projects/<项目>/*.jsonl`），**零埋点**——Skill 与 Agent 调用原生记为 `tool_use` 事件。

产出信号：调用次数 / 最近使用（冷门度）/ 重试率 / 机制级失败次数。

**关键实测结论**：
- **机制级失败结构性接近 0**：技能「跑起来但干得烂」不会记为 error。把它当质量指标会得出「一切正常」的错觉。真正的质量信号是「用户推翻/重做」，但极稀疏。
- **窗口必须 < 转录保留期**（默认 30 天）。否则资产在「够冷」之前证据已被清理，「无数据」会被读成「无例外」——**假阴性**。这是最容易静默失效的配置。
- **源会持续过期，须落快照**：窗口内证据随源清理而流失。每轮聚合追加成 append-only 快照，使时间序列不随源过期丢失；并与上一轮对比报出**退出窗口**的资产（语义中性：可能真冷门、也可能记录刚过期——正因两者不可区分，才必须显式报出，不能读成「无例外」）。
- **同名有两种记法**（裸名 / `插件:资产`），须归一化，否则冷门度被拆散误判。
- **同一项目的多份检出**（不同目录/分支）须按 git remote 归并，否则会被算成两个项目、看起来像 bug。

### B. 回归门禁（未实施）

给高价值 skill 建冻结的 golden set + LLM-judge，CI 定时跑，低于基线阈值则阻断。
**judge 模型从配置读取、不硬编码**（随环境可用模型走）。
**触发条件曾是「失败成簇」——已证不可达**（见 §六）。

### C. 漂移检测（未实施）

定时 diff 上游 marketplace 与依赖版本，变化则触发对应的回归门禁。

## 五、决策层：检测与决策分离

**自动化只 `flag`（建议），不做动作；动作永远是人**，且必须带理由 + 时效，记录成**决策日志**——决策本身也是数据，是进化的燃料。

- **双向 override**：① 评测不理想但特殊场景需暂留 → `keep` + `ttl`（到期自动重回队列，避免「暂留变永久」）；② 评测达标但人工判差不理想 → 变更 + 理由**回灌**评测，修正盲区。
- **动作自我了结**：真正把能力退役/上移后，它就不在扫描范围内，任何机器都不再报它。日志里那条只是「已决定、未执行」的中间态。
- **决策日志落点 = 本机**（不进任何仓库）：与工具「输出不过仓库」的既定原则一致；触发来自本机转录、噪声本就是本机的；写进共享仓库会让「最新一条」的先后顺序失去意义（追加式日志的合并语义不可靠）；变更理由本就该写在那个变更的 commit 里。
- **不可裁决的资产拒绝入队**：内置资产、外部 marketplace 的资产不在「需你裁决」队列里——让人决定一件他做不到的事，会毁掉整份报告的可信度。

## 六、失败模式与教训（实测得出，通用）

| 失败模式 | 表现 | 教训 |
|---|---|---|
| **证据过期 = 假阴性** | 窗口 ≥ 保留期，长期显示「无例外」，其实无数据 | 阈值必须严格小于数据保留期，并显式区分「无数据」与「无例外」 |
| **队列混入不可裁决项** | 队列要求人决定他做不到的事（如内置资产） | 入队前按**来源清点**过滤，只收真正属于使用者的资产 |
| **落了账没人读** | 裁决写了，但检测侧从不读 → 下周原样再催 | 「检测→裁决→**影响下次检测**」的闭环段必须实现，否则 ttl 只是装饰字段 |
| **触发条件不可达** | 以「等信号出现」为由推迟，而该信号结构性不会出现 | 不可达的触发条件 = **伪装成等待的不做决定**；推迟必须给可验证的事实条件 |
| **以正则扫自由文本作判据** | 误报率极高（实测 18 → 真值 2） | 判据必须读原文或语义判断，禁止正则/关键词扫自由文本 |
| **归属错误** | 同一模式的多次重犯：缺口只活在记忆里、落了账没人读、落了账但落错层 | 都是**归属问题**。顺序必须是：**先问作用域，再问载体** |
| **上游改内容不升版本** | 更新器按版本号判「已是最新」→ **静默空转**，缓存留旧内容、零报错 | 更新判定应比**内容指纹**，不依赖上游的版本纪律 |
| **标识符改名无迁移通道** | 仓库/命名空间改名后，存量环境卡在**半迁移态**（配置写新名但从未真正注册，旧名仍挂着） | 改名必须配迁移脚本；迁移 = 枚举受影响方 → 备份 → 移除旧注册 → 注册新名 → 重装，**不可只改字符串** |
| **平台专有部件静默失效** | 调度、通知等部件绑平台；换平台即无通知、无自动运行——而「没有」与「一切正常」**表现一致** | 平台专有部件要在早期验证并**显式声明边界**；静默失效的调度比「没有」更糟（让人以为监控开着） |

## 七、边界（明确不做）

- **不追求「全量」**：冷门/重试/失败只回答「谁没人用」，回答不了「谁做得好」；后者需要另建度量，且在当前数据量下是噪声。
- **不自动化语义动作**：去语境化、上移、写入归宿，都是有语义的动作，**必须人裁决**。
- **不提前抽象**：0 条记录时不建拆分机制、0 个候选时不建晋升通道。未接入项一律登记**触发条件**（见 BACKLOG），不登记日期。

## 八、经验侧：偏好与经验的分层

**与放置侧同构**，只是对象不同：

| | 放置侧（流水线 A 的晋升通道） | 经验侧 |
|---|---|---|
| 对象 | skill / agent | memory 条目（偏好、经验） |
| 现象 | 同一能力在多个项目层各存一份 | 同一偏好在多个项目 memory 各存一份 |
| 归宿 | 共享层（团队/公共 marketplace） | **全局用户层**（`~/.claude/CLAUDE.md`） |
| 判据 | **可哈希**（文件内容比对） | **不可哈希**（散文，同一规则两份措辞） |

**根因**：三层体系全是「共享」维度；而「跨我所有项目、但不共享给别人」这个维度**没有归属**。通用偏好只能就近沉在碰巧用到的项目里，于是每换一个项目就重学一遍，换设备归零。

**分三步**：

1. **建归宿**（已做）——建全局用户层文件（`~/.claude/CLAUDE.md`），把已确认的通用规则上移、去重、去项目语境。
2. **修通路**（未做）——**归宿本身不解决换设备**：`~/.claude/CLAUDE.md` 与 auto memory 一样是**机器本地**，换设备即丢。跨设备须外部机制：内容存**个人私有 git 仓库**，各设备把 `~/.claude/CLAUDE.md` 软链过去（与 `health/` 脚本同一模式）。
   - **为什么不放本仓库**：本仓库面向「任何团队装上就能用」，而全局偏好面向「我个人怎么工作」，两者受众不同；塞进来即把个人语境放进公共层（同 §二 准入判据）。
   - **注意**：`~/.claude/settings.json` 含凭证，**不进该仓库**。
3. **检测机制**（未做）——自动识别「这条偏好是通用的、该上移」。难点是**无确定性判据**（不像放置侧可哈希），候选路径：语义聚类，或把判断前移到「写入时刻」（memory 自带 scope 字段）。

**边界**：不做全量会话沉积（信号密度极低、大半是一次性事务），不做正则捕获，不自动写入（写入是有语义的动作，须人裁决）。

## 九、关键不变量（改动前必读）

1. 分层准入判据适用于该层**任何文件**。
2. 用量窗口 **<** 转录保留期（默认 30 天），否则假阴性。
3. 例外队列**只收可裁决资产**（过滤内置与外部 marketplace）。
4. 检测侧**必须读决策日志**，否则闭环断在消费端。
5. 决策日志**只存本机**，不进任何仓库。
6. 禁止正则扫自由文本作判据。
7. 冷却阈值 ≥ 窗口/保留期 → 直接报配置失效，不静默。
